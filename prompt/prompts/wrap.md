---
name: wrap
when: End of a work session, before archiving — replaces "anything else u see? any loose ends?"
---
Before we end this session, run a loose-ends sweep:
- uncommitted or unpushed work, open PRs awaiting merge
- half-finished configs (env vars, tokens, permissions) and what they block
- stale docs, README, memory files, or metadata that no longer match final state
- anything I said I'd decide and didn't

Fix anything that takes ≤2 minutes without asking. List the rest with effort estimates. Then confirm the session is archive-ready.
